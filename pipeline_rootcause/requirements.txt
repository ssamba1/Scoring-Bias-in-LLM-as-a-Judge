# Dependencies for Root Cause Pipeline  
# Install: pip install -r requirements_rootcause.txt
# Requires GPU (CUDA) for model inference
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.25.0
huggingface_hub>=0.20.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
